@{
    ViewData["Title"] = "Login";
}

<h2>Login</h2>
@if (ViewBag.Message != null)
{
    <div class="alert alert-success">@ViewBag.Message</div>
}

<p>Here you can implement login functionality...</p>